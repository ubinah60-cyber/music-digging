<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.musicdigging.mapper.UserMapper">

    <select id="findAll" resultType="com.example.musicdigging.domain.User">
        SELECT id, name
        FROM user
    </select>

</mapper>
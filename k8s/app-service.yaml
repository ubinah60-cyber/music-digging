apiVersion: v1
kind: Service

metadata:
  name: music-digging-app-service
  namespace: music-digging

spec:
  type: ClusterIP

  selector:
    app: music-digging-app

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080